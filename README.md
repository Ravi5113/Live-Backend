Backend JS (Express) for Taxi monorepo

Run locally:

```bash
cd taxi-app/backend-js
npm install
npm run dev   # requires nodemon
# or
npm start
```

Defaults:
- MongoDB: mongodb://127.0.0.1:27017/taxi_app
- PORT: 3004
- Seed admin: admin@example.com / password
